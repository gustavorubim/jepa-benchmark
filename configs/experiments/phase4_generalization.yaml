experiment:
  name: phase4_generalization
  output_dir: outputs/phase4_generalization
  seeds: [0]
env:
  id: FetchPushDense-v4
  obs_mode: state
  max_episode_steps: 50
generalization:
  perturbations:
    - observation_noise
    - goal_distribution_shift
    - action_noise
    - object_initial_position_shift
  levels: [0.0, 0.05, 0.1, 0.2]
device:
  preferred: auto
